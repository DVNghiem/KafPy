# State: KafPy v2.0 Code Quality Refactor

**Milestone:** v2.0
**Current Phase:** 02-split-worker-pool
**Started:** 2026-04-20

---

## Project Reference

**Core Value:** High-performance Rust Kafka client with idiomatic Python API

**Current Focus:** Phase 02: Split worker_pool/ module

---

## Current Position

**Phase:** 02-split-worker-pool

**Plan:** 02-01 (completed)

**Status:** Ready for 02-02

**Progress:** [Phase 02] 1/2 plans complete

---

## Performance Metrics

**Baseline clippy:** Not yet established (GATES-01 runs at phase start)
**Tests:** All passing before refactor (baseline)
**Benchmarks:** No regression baseline (GATES-03)

---

## Accumulated Context

### Key Decisions

- **Duplication extraction before structural splits** — establishes safe extraction pattern first
- **Intra-module splits before inter-module** — worker_pool split doesn't affect coordinator
- **PyO3 boundary changes last** — RuntimeBuilder extraction is additive until old implementation removed
- **Type safety last** — HandlerId newtype ripples through all modules

### Critical Pitfalls (from research)

1. **Send+Sync Guarantees** — Use `fn assert_send_sync<T: Send + Sync>()` compile-time checks
2. **Channel Semantic Changes** — Never change mpsc capacity without documenting backpressure impact
3. **PyO3 GIL Boundary** — All Python invocations must go through `spawn_blocking` or `PythonAsyncFuture`
4. **Shutdown Ordering** — Maintain `biased` directive on `select!` in `ConsumerRunner::run()`
5. **Offset Commit State Machine** — `has_terminal` gating must remain functional after split

### Open Questions

- HandlerId vs topic distinction: Are they always equal or conceptually distinct?
- NoopSink duplication: Is worker_pool NoopSink identical to observability NoopSink?
- PartitionAccumulator naming: Rename to PerPartitionBuffer?

---

## Session Continuity

**Last updated:** 2026-04-20 (roadmap created)

---

*State file created: 2026-04-20*
