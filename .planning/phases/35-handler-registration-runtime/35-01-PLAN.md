---
phase: 35-handler-registration-runtime
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - kafpy/__init__.py
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04
  - REG-05
  - REG-06
  - REG-07
must_haves:
  truths:
    - "@app.handler(topic='...', routing=...) decorates a callable as a handler"
    - "app.register_handler(topic, handler_fn, routing=...) provides explicit registration"
    - "Handler callable signature: def handler(msg: KafkaMessage, ctx: HandlerContext) -> HandlerResult"
    - "Sync, async, and batch handlers all use the same registration API (type detection)"
    - "app.start() begins consuming; app.stop() initiates graceful drain; app.run() blocks until stop()"
  artifacts:
    - path: kafpy/handlers.py
      provides: HandlerContext, HandlerResult stubs + register_handler function
      exports: HandlerContext, HandlerResult, register_handler, __all__
    - path: kafpy/consumer.py
      provides: Consumer class wrapping _kafpy.Consumer
      exports: Consumer, __all__
    - path: kafpy/runtime.py
      provides: KafPy class with start/stop/run + @app.handler decorator
      exports: KafPy, __all__
    - path: kafpy/__init__.py
      provides: Updated imports and exports for Phase 35 types
      exports: Consumer, KafPy, HandlerContext, HandlerResult
  key_links:
    - from: kafpy/__init__.py
      to: kafpy/handlers.py
      via: from .handlers import HandlerContext, HandlerResult, register_handler
    - from: kafpy/__init__.py
      to: kafpy/consumer.py
      via: from .consumer import Consumer
    - from: kafpy/__init__.py
      to: kafpy/runtime.py
      via: from .runtime import KafPy
---

<objective>
Implement handler registration and runtime for KafPy: decorator-based and explicit handler registration, HandlerContext/HandlerResult stubs, Consumer class wrapping _kafpy.Consumer, KafPy runtime with start/stop/run lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@kafpy/__init__.py
@kafpy/_kafpy.pyi
@kafpy/config.py
@.planning/phases/35-handler-registration-runtime/35-CONTEXT.md
</context>

<interfaces>
From _kafpy (Rust extension type stubs):
```python
# kafpy/_kafpy.pyi
@dataclass
class KafkaMessage:
    key: bytes | None
    value: bytes
    timestamp: int
    headers: dict[str, str]
    topic: str
    partition: int
    offset: int

class Consumer:
    def add_handler(self, topic: str, handler: Callable[[KafkaMessage], None]) -> None: ...
    def start(self) -> None: ...
    def stop(self) -> None: ...
```

From Phase 35 Context (D-01 through D-06):
- D-01: `consumer = kafpy.Consumer(config)` → Rust Consumer; `app = kafpy.KafPy(consumer)` → Python wrapper; handlers on KafPy
- D-02: Callable type detection: inspect.iscoroutinefunction/isasyncgenfunction/yield
- D-03: HandlerContext stub (frozen dataclass): topic/partition/offset/timestamp/headers
- D-04: HandlerResult stub (frozen dataclass): action field
- D-05: app.start(), app.stop(), app.run() lifecycle
- D-06: kafpy/handlers.py, kafpy/consumer.py, kafpy/runtime.py
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create kafpy/handlers.py with HandlerContext, HandlerResult stubs and register_handler</name>
  <files>kafpy/handlers.py</files>
  <action>
Create kafpy/handlers.py with the following content:

1. **Module docstring**: "Handler types and registration for KafPy."

2. **`__all__`** = ["HandlerContext", "HandlerResult", "register_handler"]

3. **`HandlerContext` class** (D-03 from Phase 35 context):
   - `@dataclass(frozen=True)`
   - Fields:
     - `topic: str`
     - `partition: int`
     - `offset: int`
     - `timestamp: int`
     - `headers: dict[str, str]`
   - This is a stub — full implementation in Phase 36 or later

4. **`HandlerResult` class** (D-04 from Phase 35 context):
   - `@dataclass(frozen=True)`
   - Fields:
     - `action: str` — "ack", "nack", "retry", "dlq" (stub — no enforcement)
   - This is a stub — full implementation in Phase 36 or later

5. **`register_handler` function** (REG-01):
   - Signature: `def register_handler(topic: str, handler: Callable, *, routing: RoutingConfig | None = None) -> None`
   - Use `inspect.iscoroutinefunction` to detect async handlers
   - Use `inspect.isasyncgenfunction` to detect async batch handlers
   - Use `inspect.isgeneratorfunction` to detect sync batch handlers
   - Store handler type as "sync", "async", or "batch"
   - Raise `ValueError` if routing_mode is invalid
   - This is a module-level function that will be wired up in Phase 35 execution

Note: Do NOT create a global handlers dict at module level. The registration state lives on the KafPy instance.
  </action>
  <verify>
python3 -c "
import ast
with open('kafpy/handlers.py') as f:
    tree = ast.parse(f.read())

# Check __all__
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for t in node.targets:
            if isinstance(t, ast.Name) and t.id == '__all__':
                names = [e.value for e in node.value.elts]
                assert set(names) == {'HandlerContext', 'HandlerResult', 'register_handler'}, f'__all__ mismatch: {names}'

# Check HandlerContext has required fields
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'HandlerContext':
        fields = [f for f in node.body if isinstance(f, ast.AnnAssign) and isinstance(f.target, ast.Name)]
        field_names = [f.target.id for f in fields]
        for fname in ['topic', 'partition', 'offset', 'timestamp', 'headers']:
            assert fname in field_names, f'{fname} missing from HandlerContext'

# Check HandlerResult has action field
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'HandlerResult':
        fields = [f for f in node.body if isinstance(f, ast.AnnAssign) and isinstance(f.target, ast.Name)]
        field_names = [f.target.id for f in fields]
        assert 'action' in field_names, 'action missing from HandlerResult'

# Check register_handler function exists
func_names = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'register_handler' in func_names, 'register_handler function missing'

print('All checks passed')
"
  </verify>
  <done>kafpy/handlers.py exists with HandlerContext, HandlerResult frozen dataclasses and register_handler function</done>
</task>

<task type="auto">
  <name>Task 2: Create kafpy/consumer.py wrapping _kafpy.Consumer</name>
  <files>kafpy/consumer.py</files>
  <action>
Create kafpy/consumer.py with the following content:

1. **Module docstring**: "Consumer wrapper for KafPy."

2. **`__all__`** = ["Consumer"]

3. **`Consumer` class** wrapping `_kafpy.Consumer`:
   - `__init__(self, config: ConsumerConfig)`:
     - Store `config`
     - Call `_kafpy.Consumer(config.to_rust())` to create the Rust consumer
   - Expose `_consumer: _kafpy.Consumer` as a private instance variable for use by KafPy
   - Implement the following proxy methods by delegating to `_consumer`:
     - `def add_handler(self, topic: str, handler: Callable) -> None` — delegate to `_consumer.add_handler`
     - `def start(self) -> None` — delegate to `_consumer.start`
     - `def stop(self) -> None` — delegate to `_consumer.stop`

Note: Do NOT import _kafpy at module level. Import it inside __init__ to avoid circular imports.
  </action>
  <verify>
python3 -c "
import ast
with open('kafpy/consumer.py') as f:
    tree = ast.parse(f.read())

# Check __all__
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for t in node.targets:
            if isinstance(t, ast.Name) and t.id == '__all__':
                names = [e.value for e in node.value.elts]
                assert names == ['Consumer'], f'__all__ should be [\"Consumer\"], got {names}'

# Check Consumer class exists
class_names = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
assert 'Consumer' in class_names, 'Consumer class missing'

# Check Consumer has __init__ that takes config
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'Consumer':
        init_methods = [n for n in node.body if isinstance(n, ast.FunctionDef) and n.name == '__init__']
        assert len(init_methods) == 1, 'Consumer must have __init__'
        # Check it has config parameter
        args = init_methods[0].args
        arg_names = [a.arg for a in args.args if a.arg != 'self']
        assert 'config' in arg_names, '__init__ must take config parameter'

# Check Consumer has add_handler, start, stop methods
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'Consumer':
        methods = [n.name for n in node.body if isinstance(n, ast.FunctionDef)]
        for m in ['add_handler', 'start', 'stop']:
            assert m in methods, f'{m} method missing from Consumer'

print('All checks passed')
"
  </verify>
  <done>kafpy/consumer.py exists with Consumer class wrapping _kafpy.Consumer</done>
</task>

<task type="auto">
  <name>Task 3: Create kafpy/runtime.py with KafPy class</name>
  <files>kafpy/runtime.py</files>
  <action>
Create kafpy/runtime.py with the following content:

1. **Module docstring**: "KafPy runtime with handler lifecycle."

2. **`__all__`** = ["KafPy"]

3. **`KafPy` class**:
   - `__init__(self, consumer: Consumer)`:
     - Store `consumer` (Python Consumer wrapper, not Rust consumer)
     - Initialize `_handlers: dict[str, dict]` to store registered handlers by topic
     - Initialize `_stopping: bool = False`
   - `start(self) -> None`:
     - Call `self.consumer.start()` to begin consuming
   - `stop(self) -> None`:
     - Set `_stopping = True`
     - Call `self.consumer.stop()` to initiate graceful drain
   - `run(self) -> None`:
     - Call `self.start()`
     - Block until `self._stopping` is True (use a simple loop or signal handling)
     - Note: This is a stub — proper async event loop integration comes later
   - `handler(self, topic: str, *, routing: RoutingConfig | None = None)` decorator:
     - Signature: `def handler(self, topic: str, *, routing: RoutingConfig | None = None) -> Callable`
     - Returns a decorator that registers a handler callable for the given topic
     - Use `inspect.iscoroutinefunction` to detect async handlers
     - Use `inspect.isasyncgenfunction` to detect async batch handlers
     - Use `inspect.isgeneratorfunction` to detect sync batch handlers
     - Store handler info in `_handlers[topic]` with keys: "fn", "routing", "type"
     - Return the decorator function
   - `register_handler(self, topic: str, handler_fn: Callable, *, routing: RoutingConfig | None = None) -> None`:
     - Explicit registration method (REG-01)
     - Detect handler type via inspect
     - Store in `_handlers[topic]`

Note: This class is a stub — proper async event loop and message dispatching comes later (Phase 36+).
  </action>
  <verify>
python3 -c "
import ast
with open('kafpy/runtime.py') as f:
    tree = ast.parse(f.read())

# Check __all__
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for t in node.targets:
            if isinstance(t, ast.Name) and t.id == '__all__':
                names = [e.value for e in node.value.elts]
                assert names == ['KafPy'], f'__all__ should be [\"KafPy\"], got {names}'

# Check KafPy class exists
class_names = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
assert 'KafPy' in class_names, 'KafPy class missing'

# Check KafPy has required methods
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'KafPy':
        methods = [n.name for n in node.body if isinstance(n, ast.FunctionDef)]
        for m in ['start', 'stop', 'run', 'handler', 'register_handler']:
            assert m in methods, f'{m} method missing from KafPy'

# Check handler is a decorator (returns callable)
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'KafPy':
        handler_methods = [n for n in node.body if isinstance(n, ast.FunctionDef) and n.name == 'handler']
        assert len(handler_methods) == 1, 'KafPy must have handler method'
        # Check it returns a value (decorator pattern)
        has_return = any(isinstance(n, ast.Return) for n in ast.walk(handler_methods[0]))
        assert has_return, 'handler method should return a decorator'

print('All checks passed')
"
  </verify>
  <done>kafpy/runtime.py exists with KafPy class with start/stop/run and @app.handler decorator</done>
</task>

<task type="auto">
  <name>Task 4: Update kafpy/__init__.py for Phase 35</name>
  <files>kafpy/__init__.py</files>
  <action>
Update kafpy/__init__.py as follows:

1. **Remove** the try/except block for HandlerContext and HandlerResult (Phase 35 now provides real implementations)

2. **Add** new imports from Phase 35 modules:
```python
from .handlers import (
    HandlerContext,
    HandlerResult,
    register_handler,
)
from .consumer import Consumer
from .runtime import KafPy
```

3. **Update** __all__ to include new Phase 35 exports:
   - Add "Consumer" from .consumer
   - Add "KafPy" from .runtime
   - Add "register_handler" from .handlers
   Keep existing items (ConsumerConfig, RoutingConfig, RetryConfig, BatchConfig, ConcurrencyConfig, KafkaMessage, ProducerConfig, Producer, HandlerContext, HandlerResult, KafPyError, ConsumerError, HandlerError, ConfigurationError)

4. **Verify** the module docstring still accurately describes the API (no changes needed to docstring)

Note: Keep the try/except for exceptions (Phase 36 provides them) and KafPy (Phase 34 stub was added, Phase 35 implements it).
  </action>
  <verify>
python3 -c "
import ast
with open('kafpy/__init__.py') as f:
    content = f.read()
    tree = ast.parse(content)

# Check new imports exist
imports = {}
for node in ast.walk(tree):
    if isinstance(node, ast.ImportFrom):
        module = node.module
        names = [a.name for a in node.names]
        imports[module] = names

assert '.handlers' in imports, 'handlers module not imported'
assert '.consumer' in imports, 'consumer module not imported'
assert '.runtime' in imports, 'runtime module not imported'

# Check handlers imports
for name in ['HandlerContext', 'HandlerResult', 'register_handler']:
    assert name in imports.get('.handlers', []), f'{name} not in handlers imports'

# Check Consumer and KafPy in imports
assert 'Consumer' in imports.get('.consumer', []), 'Consumer not imported from consumer'
assert 'KafPy' in imports.get('.runtime', []), 'KafPy not imported from runtime'

# Check __all__ has new items
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for t in node.targets:
            if isinstance(t, ast.Name) and t.id == '__all__':
                names = [e.value for e in node.value.elts]
                for item in ['Consumer', 'KafPy', 'register_handler', 'HandlerContext', 'HandlerResult']:
                    assert item in names, f'{item} missing from __all__'

# Check try/except for HandlerContext/HandlerResult is gone
assert 'HandlerContext = None' not in content, 'HandlerContext stub should be removed'
assert 'HandlerResult = None' not in content, 'HandlerResult stub should be removed'

print('All checks passed')
"
  </verify>
  <done>kafpy/__init__.py updated with imports from handlers, consumer, runtime modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {Python handler} → {KafPy runtime} | Handler callable registered and called by KafPy |
| {KafPy runtime} → {Rust consumer} | Consumer wrapper delegates to Rust consumer |
| {Rust consumer} → {Kafka} | Message consumption at Rust level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | KafPy.handler() decorator | mitigate | Validate topic is non-empty string before registration |
| T-35-02 | Tampering | register_handler() | mitigate | Validate callable is actually callable before storing |
</threat_model>

<verification>
python3 -c "
# Verify all Phase 35 modules exist and are importable (syntax check)
import ast

for path in ['kafpy/handlers.py', 'kafpy/consumer.py', 'kafpy/runtime.py']:
    with open(path) as f:
        ast.parse(f.read())
    print(f'{path}: syntax OK')

# Verify __init__.py syntax
with open('kafpy/__init__.py') as f:
    ast.parse(f.read())
print('kafpy/__init__.py: syntax OK')

# Verify all required classes/functions exist
for path, expected in [
    ('kafpy/handlers.py', ['HandlerContext', 'HandlerResult', 'register_handler']),
    ('kafpy/consumer.py', ['Consumer']),
    ('kafpy/runtime.py', ['KafPy']),
]:
    with open(path) as f:
        content = f.read()
    for name in expected:
        assert name in content, f'{name} not found in {path}'

print('All Phase 35 files verified')
"
</verification>

<success_criteria>
1. kafpy/handlers.py exists with HandlerContext (frozen dataclass with topic/partition/offset/timestamp/headers), HandlerResult (frozen dataclass with action), and register_handler function
2. kafpy/consumer.py exists with Consumer class wrapping _kafpy.Consumer (has add_handler/start/stop methods)
3. kafpy/runtime.py exists with KafPy class having start(), stop(), run(), handler() decorator, and register_handler() explicit method
4. kafpy/__init__.py imports and exports Consumer, KafPy, HandlerContext, HandlerResult, register_handler from new modules
5. Handler type detection uses inspect.iscoroutinefunction/isasyncgenfunction/isgeneratorfunction
6. All classes use @dataclass(frozen=True)
7. No try/except stubs remain for HandlerContext/HandlerResult in __init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/35-handler-registration-runtime/35-01-SUMMARY.md`
</output>
