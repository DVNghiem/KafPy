---
phase: 27-shutdown-drain-polish
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Shutdown drain: batch_worker_loop Branch 3 flushes all partitions, invokes handler for each batch, then breaks"
    - "All 4 HandlerMode paths route correctly from WorkerPool::new"
    - "GIL invariant: spawn_blocking acquires GIL only inside blocking task; PythonAsyncFuture releases GIL before returning Pending"
    - "cargo check passes with no errors"
  artifacts:
    - path: "src/worker_pool/mod.rs"
      provides: "batch_worker_loop Branch 3 drain implementation (lines 655-688)"
    - path: "src/worker_pool/mod.rs"
      provides: "WorkerPool::new routing logic for all 4 HandlerModes (lines 855-900)"
    - path: "src/python/handler.rs"
      provides: "invoke/invoke_batch spawn_blocking GIL pattern (lines 160-196, 224-262)"
    - path: "src/python/async_bridge.rs"
      provides: "PythonAsyncFuture transient GIL pattern (lines 41-81)"
  key_links:
    - from: "WorkerPool::new (mod.rs:855-900)"
      to: "batch_worker_loop / worker_loop"
      via: "use_batch flag routes BatchSync/BatchAsync to batch loop"
    - from: "worker_loop (mod.rs:416-418)"
      to: "shutdown_token.cancelled()"
      via: "Branch checks is_cancelled() after active_message processed"
    - from: "batch_worker_loop (mod.rs:655-688)"
      to: "accumulator.flush_all() + invoke_mode_batch + break"
      via: "Branch 3: drain accumulated batches on shutdown signal"
    - from: "PythonAsyncFuture::poll_coroutine (async_bridge.rs:41-81)"
      to: "Python GIL"
      via: "Python::attach holds GIL only during coro.send(None), releases before returning"
---

<objective>
Verify all Phase 24-26 implementation is correct and integrated. Phase 27 is verification-only — no new code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-shutdown-drain-polish/27-CONTEXT.md
@src/worker_pool/mod.rs
@src/python/handler.rs
@src/python/async_bridge.rs
</context>

<tasks>

<task type="auto">
  <name>Verify shutdown drain — batch_worker_loop Branch 3</name>
  <files>src/worker_pool/mod.rs</files>
  <action>
Read src/worker_pool/mod.rs lines 655-688. Verify Branch 3 of batch_worker_loop:
1. Triggered by `_ = shutdown_token.cancelled()`
2. Calls `accumulator.flush_all()` to get all `(partition, Vec<OwnedMessage>)` pairs
3. For each nonempty batch: creates ExecutionContext, calls `handler.invoke_mode_batch()`, awaits result via `handle_batch_result_inline()`
4. After processing all partitions: `break` to exit the loop

Confirm the pattern matches the comment at line 656-659:
"batch worker: shutdown signal, draining"

If all correct, the drain is correctly implemented per D-01.
  </action>
  <verify>
<code_verify>
grep -n "shutdown_token.cancelled\|flush_all\|invoke_mode_batch\|handle_batch_result_inline" src/worker_pool/mod.rs | head -30
</code_verify>
  </verify>
  <done>Branch 3 drain verified: flush_all() called, each batch invoked via invoke_mode_batch(), loop breaks after drain</done>
</task>

<task type="auto">
  <name>Verify all 4 HandlerMode paths route correctly from WorkerPool::new</name>
  <files>src/worker_pool/mod.rs</files>
  <action>
Read src/worker_pool/mod.rs lines 855-900. Verify WorkerPool::new routing:

1. Line 856-861: `handler.mode()` checked; `use_batch = true` iff mode is BatchSync or BatchAsync
2. Line 874-886 (batch path): `batch_worker_loop` spawned when `use_batch == true`
3. Line 888-900 (single path): `worker_loop` spawned when `use_batch == false`

This means:
- SingleSync → worker_loop
- SingleAsync → worker_loop (same loop, invoke_mode routes to invoke_async)
- BatchSync → batch_worker_loop
- BatchAsync → batch_worker_loop (batch loop routes to invoke_batch_async)

Confirm there is no other routing logic that could redirect any mode.
  </action>
  <verify>
<code_verify>
grep -n "use_batch\|BatchSync\|BatchAsync\|batch_worker_loop\|worker_loop" src/worker_pool/mod.rs
</code_verify>
  </verify>
  <done>Routing verified: BatchSync/BatchAsync → batch_worker_loop; SingleSync/SingleAsync → worker_loop</done>
</task>

<task type="auto">
  <name>Verify GIL invariant — spawn_blocking (sync) and PythonAsyncFuture (async)</name>
  <files>src/python/handler.rs, src/python/async_bridge.rs</files>
  <action>
Verify GIL is held transiently only during Python execution, never across Rust-side orchestration.

**Sync path (invoke / invoke_batch):**

Read handler.rs lines 160-196 (invoke) and 224-262 (invoke_batch):
- `spawn_blocking` wraps the entire blocking task
- Inside the closure, `Python::attach(|py| { ... })` acquires GIL
- All Python work (callback.call1) happens inside the `Python::attach` scope
- GIL is released when `Python::attach` returns (end of closure)
- Rust-side orchestration (building PyDict, spawning task) happens BEFORE spawn_blocking

**Async path (invoke_async / invoke_batch_async):**

Read async_bridge.rs lines 41-81 (poll_coroutine):
- `poll_coroutine` acquires GIL via `Python::attach`
- Calls `self.coro.call_method1(py, "send", ...)` while GIL is held
- Immediately returns `Poll::Pending` or `Poll::Ready` after the attach scope
- GIL is NOT held across the await point — it is released before returning Pending
- Waker is registered (waker.wake()) so Tokio re-polls when ready

Confirm this matches D-02: "GIL held transiently only during Python execution"
  </action>
  <verify>
<code_verify>
# Verify spawn_blocking GIL scope
grep -n "spawn_blocking\|Python::attach" src/python/handler.rs

# Verify PythonAsyncFuture transient GIL
grep -n "poll_coroutine\|Python::attach\|Pending" src/python/async_bridge.rs
</code_verify>
  </verify>
  <done>GIL invariant confirmed: spawn_blocking holds GIL only inside Python::attach; PythonAsyncFuture releases GIL before returning Pending</done>
</task>

<task type="auto">
  <name>Verify cargo check passes</name>
  <files>src/worker_pool/mod.rs, src/python/handler.rs, src/python/async_bridge.rs</files>
  <action>
Run `cargo check` in the project root to verify all code compiles without errors.

This confirms the integration of Phases 24-26 did not introduce any type errors, missing imports, or trait bound violations.
  </action>
  <verify>
<automated>cargo check 2>&1</automated>
  </verify>
  <done>cargo check passes with no errors or warnings</done>
</task>

</tasks>

<verification>
## Phase 27 Verification Checklist

- [ ] batch_worker_loop Branch 3 (lines 655-688): flush_all() + invoke_mode_batch() + break
- [ ] WorkerPool::new routing (lines 855-900): BatchSync/BatchAsync → batch loop; SingleSync/SingleAsync → worker loop
- [ ] GIL sync: spawn_blocking + Python::attach holds GIL only inside callback
- [ ] GIL async: PythonAsyncFuture::poll_coroutine releases GIL before returning Pending
- [ ] cargo check passes
</verification>

<success_criteria>
Phase 27 is complete when all 4 verification tasks pass:
1. Shutdown drain verified in Branch 3
2. All 4 HandlerMode paths route correctly from WorkerPool::new
3. GIL invariant confirmed for both sync and async paths
4. cargo check passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-shutdown-drain-polish/27-01-SUMMARY.md`
</output>
