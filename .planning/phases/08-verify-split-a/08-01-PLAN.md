---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-split-worker-pool/VERIFICATION.md
autonomous: true
requirements:
  - SPLIT-A-01
  - SPLIT-A-02
  - SPLIT-A-03
  - SPLIT-A-04
  - SPLIT-A-05
  - SPLIT-A-06
---

<objective>
Record SPLIT-A-01 through SPLIT-A-06 verification results in VERIFICATION.md for Phase 02.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Verify SPLIT-A-01 and SPLIT-A-05</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>src/worker_pool/accumulator.rs</read_first>
  <action>
grep -n "struct PerPartitionBuffer" src/worker_pool/accumulator.rs
grep -n "PartitionAccumulator" src/worker_pool/mod.rs
Append SPLIT-A-01 and SPLIT-A-05 results to VERIFICATION.md
  </action>
  <verify>
grep -n "struct PerPartitionBuffer" src/worker_pool/accumulator.rs | head -1
  </verify>
  <done>
SPLIT-A-01 (PerPartitionBuffer in accumulator.rs) and SPLIT-A-05 (renamed from PartitionAccumulator) verified
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify SPLIT-A-02</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>src/worker_pool/batch_loop.rs</read_first>
  <action>
grep -n "fn batch_worker_loop" src/worker_pool/batch_loop.rs
Append SPLIT-A-02 result to VERIFICATION.md
  </action>
  <verify>
grep -n "fn batch_worker_loop" src/worker_pool/batch_loop.rs | head -1
  </verify>
  <done>
SPLIT-A-02 (batch_worker_loop in batch_loop.rs) verified
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify SPLIT-A-03</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>src/worker_pool/worker.rs</read_first>
  <action>
grep -n "fn worker_loop" src/worker_pool/worker.rs
Append SPLIT-A-03 result to VERIFICATION.md
  </action>
  <verify>
grep -n "fn worker_loop" src/worker_pool/worker.rs | head -1
  </verify>
  <done>
SPLIT-A-03 (worker_loop in worker.rs) verified
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify SPLIT-A-04</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>src/worker_pool/pool.rs</read_first>
  <action>
grep -n "struct WorkerPool" src/worker_pool/pool.rs
Append SPLIT-A-04 result to VERIFICATION.md
  </action>
  <verify>
grep -n "struct WorkerPool" src/worker_pool/pool.rs | head -1
  </verify>
  <done>
SPLIT-A-04 (WorkerPool in pool.rs) verified
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify SPLIT-A-06</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>src/python/batch.rs</read_first>
  <action>
grep -n "struct BatchAccumulator" src/python/batch.rs
Append SPLIT-A-06 result to VERIFICATION.md
  </action>
  <verify>
grep -n "struct BatchAccumulator" src/python/batch.rs | head -1
  </verify>
  <done>
SPLIT-A-06 (BatchAccumulator in python/batch.rs) verified
  </done>
</task>

<task type="auto">
  <name>Task 6: Finalize VERIFICATION.md</name>
  <files>.planning/phases/02-split-worker-pool/VERIFICATION.md</files>
  <read_first>.planning/phases/02-split-worker-pool/VERIFICATION.md</read_first>
  <action>
Ensure VERIFICATION.md has complete frontmatter and all 6 SPLIT-A results.
Run cargo clippy --all-targets 2>&1 | tail -3
Append final status: PASS
  </action>
  <verify>
test -f .planning/phases/02-split-worker-pool/VERIFICATION.md
  </verify>
  <done>
VERIFICATION.md complete with all 6 requirements verified PASS
  </done>
</task>

</tasks>

<verification>
## Phase-level checks
1. VERIFICATION.md exists in Phase 02 directory
2. All 6 SPLIT-A requirements show PASS status
3. cargo clippy --all-targets passes
</verification>

<success_criteria>
- SPLIT-A-01: PerPartitionBuffer in src/worker_pool/accumulator.rs
- SPLIT-A-02: batch_worker_loop in src/worker_pool/batch_loop.rs
- SPLIT-A-03: worker_loop in src/worker_pool/worker.rs
- SPLIT-A-04: WorkerPool in src/worker_pool/pool.rs
- SPLIT-A-05: PartitionAccumulator renamed to PerPartitionBuffer
- SPLIT-A-06: BatchAccumulator in src/python/batch.rs
- VERIFICATION.md created with PASS status
</success_criteria>
