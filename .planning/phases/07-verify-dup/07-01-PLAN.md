---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-extract-duplication/VERIFICATION.md
autonomous: true
requirements:
  - DUP-01
  - DUP-02
  - DUP-03
  - DUP-04

must_haves:
  truths:
    - "DUP-01 handle_execution_failure is defined and called"
    - "DUP-02 message_to_pydict is defined and called in python/handler.rs"
    - "DUP-03 flush_partition_batch is defined and called in batch_loop.rs"
    - "DUP-04 NoopSink is consolidated in observability/metrics.rs"
  artifacts:
    - path: ".planning/phases/01-extract-duplication/VERIFICATION.md"
      provides: "Formal verification record for DUP-01 through DUP-04"
      min_lines: 50
  key_links:
    - from: "VERIFICATION.md"
      to: "src/worker_pool/mod.rs"
      via: "DUP-01 verification grep"
    - from: "VERIFICATION.md"
      to: "src/python/handler.rs"
      via: "DUP-02 verification grep"
    - from: "VERIFICATION.md"
      to: "src/worker_pool/batch_loop.rs"
      via: "DUP-03 verification grep"
    - from: "VERIFICATION.md"
      to: "src/observability/metrics.rs"
      via: "DUP-04 verification grep"
---

<objective>
Formally verify DUP-01 through DUP-04 requirements from Phase 01 are present in the codebase, and produce a VERIFICATION.md that records the results. All 4 requirements are confirmed implemented; the gap analysis had incorrect grep results.

Purpose: Close the gap between Phase 01 execution claims and actual codebase state.
Output: .planning/phases/01-extract-duplication/VERIFICATION.md with formal verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/worker_pool/mod.rs (lines 21-80 for ExecutionAction enum and handle_execution_failure definition)
@src/worker_pool/batch_loop.rs (lines 28-40 for flush_partition_batch definition)
@src/python/handler.rs (lines 20-30 for message_to_pydict definition)
@src/observability/metrics.rs (lines 90-100 for NoopSink definition)
@.planning/phases/01-extract-duplication/01-01-SUMMARY.md
@.planning/phases/01-extract-duplication/01-01-PLAN.md

## Actual Locations (CORRECTED from CONTEXT.md)

| Requirement | Function | Correct File | Line |
|-------------|----------|--------------|------|
| DUP-01 | handle_execution_failure | src/worker_pool/mod.rs | 37 |
| DUP-02 | message_to_pydict | src/python/handler.rs | 23 |
| DUP-03 | flush_partition_batch | src/worker_pool/batch_loop.rs | 30 |
| DUP-04 | NoopSink | src/observability/metrics.rs | 93 |

Note: DUP-03 is in batch_loop.rs (not mod.rs) because Phase 02 split the worker_pool module after Phase 01 executed. This is the correct location post-refactoring.

## Verification Grep Commands

Each grep targets the CORRECT file based on actual codebase state.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DUP-01 (handle_execution_failure)</name>
  <files>.planning/phases/01-extract-duplication/VERIFICATION.md</files>
  <read_first>src/worker_pool/mod.rs (lines 21-80 for ExecutionAction enum and handle_execution_failure)</read_first>
  <action>
Run the following verification commands and record results in VERIFICATION.md:

1. Verify function definition exists:
   grep -n "pub(crate) async fn handle_execution_failure" src/worker_pool/mod.rs
   Expected: Line 37

2. Verify ExecutionAction enum exists:
   grep -n "pub enum ExecutionAction" src/worker_pool/mod.rs
   Expected: Line 25

3. Verify call sites exist (in worker.rs):
   grep -n "handle_execution_failure" src/worker_pool/worker.rs
   Expected: 2 call sites (Error and Rejected branches at lines 162, 197)

4. Run clippy:
   cargo clippy --all-targets 2>&1 | grep -E "error|warning" | head -20

5. Run tests:
   cargo test --lib 2>&1 | tail -10

Create .planning/phases/01-extract-duplication/VERIFICATION.md with the following structure:
- Header with phase, milestone, date, status
- DUP-01 verification section with grep command, expected output, actual output, pass/fail
- DUP-02 verification section
- DUP-03 verification section
- DUP-04 verification section
- Final status: PASS (all 4 verified)
</action>
  <verify>
    <automated>grep -n "pub(crate) async fn handle_execution_failure" src/worker_pool/mod.rs | head -1</automated>
  </verify>
  <done>
    VERIFICATION.md created with DUP-01 section showing function definition at line 37 and 2 call sites in worker.rs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify DUP-02 (message_to_pydict)</name>
  <files>.planning/phases/01-extract-duplication/VERIFICATION.md</files>
  <read_first>src/python/handler.rs (lines 20-30 for message_to_pydict definition)</read_first>
  <action>
Run the following verification commands for DUP-02:

1. Verify function definition in python/handler.rs:
   grep -n "fn message_to_pydict" src/python/handler.rs
   Expected: Line 23

2. Verify 4 call sites in handler.rs:
   grep -n "message_to_pydict" src/python/handler.rs
   Expected: Line 23 (definition) + lines 227, 285, 330, 361 (4 call sites)

3. Verify NOT in worker_pool/mod.rs (correction from CONTEXT.md):
   grep "message_to_pydict" src/worker_pool/mod.rs
   Expected: empty (function is in python/handler.rs, not worker_pool/mod.rs)

Append DUP-02 section to VERIFICATION.md with pass result.
</action>
  <verify>
    <automated>grep -n "fn message_to_pydict" src/python/handler.rs | head -1</automated>
  </verify>
  <done>
    VERIFICATION.md DUP-02 section shows message_to_pydict defined at line 23 in python/handler.rs with 4 call sites.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify DUP-03 (flush_partition_batch)</name>
  <files>.planning/phases/01-extract-duplication/VERIFICATION.md</files>
  <read_first>src/worker_pool/batch_loop.rs (lines 28-45 for flush_partition_batch definition)</read_first>
  <action>
Run the following verification commands for DUP-03:

1. Verify function definition in batch_loop.rs (NOT mod.rs - Phase 02 split moved it):
   grep -n "pub(crate) async fn flush_partition_batch" src/worker_pool/batch_loop.rs
   Expected: Line 30

2. Verify 6 call sites in batch_loop.rs:
   grep -n "flush_partition_batch" src/worker_pool/batch_loop.rs
   Expected: Line 30 (definition) + lines 134, 165, 188, 212, 237, 265 (6 call sites)

3. Verify NOT in worker_pool/mod.rs:
   grep "flush_partition_batch" src/worker_pool/mod.rs
   Expected: empty (correctly moved to batch_loop.rs during Phase 02 split)

Append DUP-03 section to VERIFICATION.md noting the file location change is correct (Phase 02 refactoring moved batch_worker_loop and its helper to batch_loop.rs).
</action>
  <verify>
    <automated>grep -n "pub(crate) async fn flush_partition_batch" src/worker_pool/batch_loop.rs | head -1</automated>
  </verify>
  <done>
    VERIFICATION.md DUP-03 section shows flush_partition_batch defined at line 30 in batch_loop.rs with 6 call sites.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify DUP-04 (NoopSink consolidation)</name>
  <files>.planning/phases/01-extract-duplication/VERIFICATION.md</files>
  <read_first>src/observability/metrics.rs (lines 88-100 for NoopSink definition)</read_first>
  <action>
Run the following verification commands for DUP-04:

1. Verify NoopSink exists in observability/metrics.rs:
   grep -n "pub struct NoopSink" src/observability/metrics.rs
   Expected: Line 93

2. Verify NoopSink is NOT defined in worker_pool/mod.rs:
   grep "struct NoopSink" src/worker_pool/mod.rs
   Expected: empty (moved to observability)

3. Verify import in worker_pool/mod.rs:
   grep "use.*NoopSink" src/worker_pool/mod.rs
   Expected: Shows import from observability module

Append DUP-04 section to VERIFICATION.md with pass result.
</action>
  <verify>
    <automated>grep -n "pub struct NoopSink" src/observability/metrics.rs | head -1</automated>
  </verify>
  <done>
    VERIFICATION.md DUP-04 section shows NoopSink consolidated in observability/metrics.rs.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create final VERIFICATION.md</name>
  <files>.planning/phases/01-extract-duplication/VERIFICATION.md</files>
  <read_first>.planning/phases/01-extract-duplication/VERIFICATION.md (all sections from Tasks 1-4)</read_first>
  <action>
Create the complete VERIFICATION.md file with:

1. Frontmatter:
   ---
   milestone: v2.0
   phase: 01-extract-duplication
   status: PASS
   date: 2026-04-25
   verified_by: Phase 07 gap closure
   ---

2. Executive Summary: All 4 DUP requirements (DUP-01 through DUP-04) verified present in codebase.

3. Verification Results Table:
   | REQ | Status | Location | Verification |
   |-----|--------|----------|--------------|
   | DUP-01 | PASS | src/worker_pool/mod.rs:37 | grep verified |
   | DUP-02 | PASS | src/python/handler.rs:23 | grep verified |
   | DUP-03 | PASS | src/worker_pool/batch_loop.rs:30 | grep verified |
   | DUP-04 | PASS | src/observability/metrics.rs:93 | grep verified |

4. Detailed sections for each requirement showing:
   - Requirement text
   - Verification grep command
   - Expected output
   - Actual output (from running commands)
   - Pass/fail status

5. Notes section correcting CONTEXT.md discrepancies:
   - DUP-02: message_to_pydict is in python/handler.rs (CONTEXT.md incorrectly said worker_pool/mod.rs)
   - DUP-03: flush_partition_batch is in batch_loop.rs (CONTEXT.md said mod.rs - correct after Phase 02 split)

6. Final status: PASS - all requirements verified

Run cargo clippy and cargo test to confirm no regressions, append results to VERIFICATION.md.
</action>
  <verify>
    <automated>cargo clippy --all-targets 2>&1 | tail -5 && cargo test --lib 2>&1 | tail -5</automated>
  </verify>
  <done>
    VERIFICATION.md exists at .planning/phases/01-extract-duplication/VERIFICATION.md with all 4 DUP requirements verified PASS.
  </done>
</task>

</tasks>

<verification>
## Phase-level checks

After all tasks complete:
1. VERIFICATION.md exists with all 4 DUP requirements verified
2. cargo clippy --all-targets passes with no new warnings
3. cargo test --lib passes with all tests passing
4. All grep verification commands return expected results
</verification>

<success_criteria>
- DUP-01: handle_execution_failure defined at line 37 in worker_pool/mod.rs, 2 call sites in worker.rs
- DUP-02: message_to_pydict defined at line 23 in python/handler.rs, 4 call sites in handler.rs
- DUP-03: flush_partition_batch defined at line 30 in batch_loop.rs, 6 call sites in batch_loop.rs
- DUP-04: NoopSink defined at line 93 in observability/metrics.rs, not in worker_pool/mod.rs
- VERIFICATION.md created with formal pass status for all 4 requirements
</success_criteria>

<output>
After completion, VERIFICATION.md is created at .planning/phases/01-extract-duplication/VERIFICATION.md
</output>
