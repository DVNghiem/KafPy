---
phase: "02-split-worker-pool"
plan: "02"
type: "execute"
wave: "2"
depends_on: ["02-01"]
files_modified:
  - "src/worker_pool/worker.rs"
  - "src/worker_pool/batch_loop.rs"
  - "src/worker_pool/pool.rs"
  - "src/worker_pool/mod.rs"
autonomous: true
requirements: ["SPLIT-A-02", "SPLIT-A-03", "SPLIT-A-04"]
must_haves:
  truths:
    - "worker_loop() function lives in worker_pool/worker.rs and compiles with all dependencies"
    - "batch_worker_loop() and handle_batch_result_inline() live in worker_pool/batch_loop.rs"
    - "WorkerPool struct lives in worker_pool/pool.rs"
    - "worker_pool/mod.rs is a thin re-export layer with no struct/impl definitions"
  artifacts:
    - path: "src/worker_pool/worker.rs"
      provides: "worker_loop async function"
      min_lines: 225
    - path: "src/worker_pool/batch_loop.rs"
      provides: "batch_worker_loop and handle_batch_result_inline"
      min_lines: 150
    - path: "src/worker_pool/pool.rs"
      provides: "WorkerPool struct and impl"
      min_lines: 140
    - path: "src/worker_pool/mod.rs"
      provides: "Thin re-export layer"
      max_lines: 50
  key_links:
    - from: "src/worker_pool/mod.rs"
      to: "src/worker_pool/worker.rs"
      via: "pub use worker::worker_loop"
      pattern: "worker_loop"
    - from: "src/worker_pool/mod.rs"
      to: "src/worker_pool/batch_loop.rs"
      via: "pub use batch_loop"
      pattern: "batch_worker_loop|handle_batch_result_inline"
    - from: "src/worker_pool/mod.rs"
      to: "src/worker_pool/pool.rs"
      via: "pub use pool::WorkerPool"
      pattern: "WorkerPool"
---

<objective>
Extract worker_loop(), batch_worker_loop(), handle_batch_result_inline(), and WorkerPool from worker_pool/mod.rs into their own files.

- SPLIT-A-02: Extract batch_worker_loop() to worker_pool/batch_loop.rs
- SPLIT-A-03: Extract worker_loop() to worker_pool/worker.rs
- SPLIT-A-04: Extract WorkerPool to worker_pool/pool.rs

Purpose: Complete the worker_pool/ split — thin mod.rs re-exports only.
Output: worker.rs + batch_loop.rs + pool.rs + thin mod.rs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## After Wave 1, worker_pool/mod.rs contains

Based on Wave 1 (Plan 02-01):
- `ExecutionAction` enum + `handle_execution_failure()` — already extracted in Phase 1
- `flush_partition_batch()` — already extracted in Phase 1
- `NoopSink` — already moved to observability/
- `PartitionAccumulator` → REMOVED (renamed to PerPartitionBuffer in accumulator.rs)
- `BatchAccumulator` → REMOVED (moved to python/batch.rs)
- REMAINING: `worker_loop()` (lines ~289-515), `batch_worker_loop()` (lines ~566-766), `handle_batch_result_inline()` (lines ~767-915), `WorkerPool` (lines ~916-1055)

## Import dependencies

Both `worker_loop` and `batch_worker_loop` use:
- `crate::dispatcher::OwnedMessage`
- `crate::coordinator::OffsetCoordinator`
- `crate::python::execution_result::{BatchExecutionResult, ExecutionResult}`
- `crate::python::executor::Executor`
- `crate::python::handler::PythonHandler`
- `crate::python::context::ExecutionContext`
- `crate::dlq::{DlqMetadata, DlqRouter, SharedDlqProducer}`
- `crate::failure::FailureReason`
- `crate::coordinator::retry_coordinator::RetryCoordinator`
- `crate::coordinator::shutdown::ShutdownCoordinator`
- `crate::dispatcher::queue_manager::QueueManager`
- `crate::observability::metrics::{HandlerMetrics, MetricLabels}`
- `crate::observability::runtime_snapshot::WorkerPoolState`
- `crate::observability::tracing::KafpySpanExt`
- `crate::python::batch::BatchAccumulator` (per Wave 1)
- `crate::worker_pool::accumulator::PerPartitionBuffer` (per Wave 1)

WorkerPool additionally uses:
- `tokio::select`
- `tokio::sync::mpsc`
- `tokio::task::JoinSet`
- `tokio_util::sync::CancellationToken`
- `Arc<QueueManager>`, `Arc<dyn Executor>`, `Arc<OffsetCoordinator>`, etc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract worker_loop() to worker_pool/worker.rs</name>
  <files>src/worker_pool/worker.rs</files>
  <action>
Read the current src/worker_pool/mod.rs and locate the full `worker_loop` function (it starts at the `async fn worker_loop` declaration and ends where the next `async fn` begins).

Create src/worker_pool/worker.rs with:
1. Module doc comment: `//! Worker loop — polls messages and invokes the Python handler.`
2. All required imports (match what worker_loop uses in mod.rs)
3. The complete `worker_loop` async function body (copy verbatim from mod.rs)

The worker_loop signature uses:
```rust
async fn worker_loop(
    mut rx: mpsc::Receiver<OwnedMessage>,
    executor: Arc<dyn Executor>,
    offset_coordinator: Arc<dyn OffsetCoordinator>,
    retry_coordinator: Arc<RetryCoordinator>,
    dlq_producer: Arc<SharedDlqProducer>,
    dlq_router: Arc<dyn DlqRouter>,
    queue_manager: Arc<QueueManager>,
    shutdown_token: CancellationToken,
) {
```

DO NOT change any logic inside worker_loop — copy verbatim. The function body references handle_execution_failure, flush_partition_batch, ExecutionAction, etc. which are still in worker_pool/mod.rs.

After creating the file, add `pub mod worker;` to worker_pool/mod.rs near the top with the other `pub mod` declarations.
</action>
  <verify>
    <automated>grep -c "async fn worker_loop" /home/nghiem/project/KafPy/src/worker_pool/worker.rs && grep -c "^pub mod" /home/nghiem/project/KafPy/src/worker_pool/mod.rs | head -1</automated>
  </verify>
  <done>worker_loop async function exists in worker_pool/worker.rs with all imports, pub mod worker added to mod.rs</done>
</task>

<task type="auto">
  <name>Task 2: Extract batch_worker_loop() + handle_batch_result_inline() to worker_pool/batch_loop.rs</name>
  <files>src/worker_pool/batch_loop.rs</files>
  <action>
Read the current src/worker_pool/mod.rs and locate:
1. `batch_worker_loop` function (from `async fn batch_worker_loop` to before `handle_batch_result_inline`)
2. `handle_batch_result_inline` function (from its declaration to before WorkerPool struct)

Create src/worker_pool/batch_loop.rs with:
1. Module doc comment: `//! Batch worker loop and result handler.`
2. All required imports (match what both functions use)
3. The complete `batch_worker_loop` async function
4. The complete `handle_batch_result_inline` async function

batch_worker_loop signature:
```rust
async fn batch_worker_loop(
    rx: mpsc::Receiver<OwnedMessage>,
    executor: Arc<dyn Executor>,
    offset_coordinator: Arc<dyn OffsetCoordinator>,
    retry_coordinator: Arc<RetryCoordinator>,
    dlq_producer: Arc<SharedDlqProducer>,
    dlq_router: Arc<dyn DlqRouter>,
    queue_manager: Arc<QueueManager>,
    shutdown_token: CancellationToken,
    batch_accumulator: Arc<BatchAccumulator>,
)
```

handle_batch_result_inline signature:
```rust
async fn handle_batch_result_inline(
    results: Vec<(OwnedMessage, ExecutionResult)>,
    ctx: &ExecutionContext,
    retry_coordinator: Arc<RetryCoordinator>,
    dlq_producer: Arc<SharedDlqProducer>,
    dlq_router: Arc<dyn DlqRouter>,
    queue_manager: Arc<QueueManager>,
) -> Result<(), ()>
```

Both functions reference flush_partition_batch and handle_execution_failure (still in mod.rs), PerPartitionBuffer and BatchAccumulator (from Wave 1 modules), etc.

After creating the file, add `pub mod batch_loop;` to worker_pool/mod.rs.
</action>
  <verify>
    <automated>grep -c "async fn batch_worker_loop" /home/nghiem/project/KafPy/src/worker_pool/batch_loop.rs && grep -c "async fn handle_batch_result_inline" /home/nghiem/project/KafPy/src/worker_pool/batch_loop.rs</automated>
  </verify>
  <done>batch_worker_loop and handle_batch_result_inline exist in worker_pool/batch_loop.rs, pub mod batch_loop added to mod.rs</done>
</task>

<task type="auto">
  <name>Task 3: Extract WorkerPool to worker_pool/pool.rs and thin mod.rs</name>
  <files>src/worker_pool/pool.rs, src/worker_pool/mod.rs</files>
  <action>
Read the current src/worker_pool/mod.rs and locate the WorkerPool struct (starts `pub struct WorkerPool`) and its full impl block (ends at the test section starting `// ─── Tests`).

Create src/worker_pool/pool.rs with:
1. Module doc comment: `//! WorkerPool — manages N Tokio workers polling handler queues.`
2. All required imports
3. The `WorkerPool` struct and its complete impl block

WorkerPool struct signature:
```rust
pub struct WorkerPool {
    workers: JoinSet<()>,
    handler_rx: mpsc::Receiver<OwnedMessage>,
    shutdown_token: CancellationToken,
    // ... fields as they appear in the struct
}
```

impl WorkerPool {
    pub fn new(...) -> Self { ... }
    pub async fn spawn(...){ ... }
    pub async fn shutdown(...){ ... }
    fn spawn_worker(...){ ... }
}

After creating pool.rs, add `pub mod pool;` and `pub use pool::WorkerPool;` to worker_pool/mod.rs.

Then thin worker_pool/mod.rs:
- Remove the `worker_loop`, `batch_worker_loop`, `handle_batch_result_inline`, and `WorkerPool` definitions
- Keep only: module doc, use statements, pub mod/re-exports, ExecutionAction, handle_execution_failure
- The file should be under 50 lines of actual code (imports + re-exports only)

Target mod.rs structure after thinning:
```
//! WorkerPool — manages N Tokio workers polling handler queues. (shortened doc)
//! (All the long implementation doc goes to pool.rs now)

use [all imports from original]

// ─── Execution Action ─────────────────────────────────────────────────────────
[ExecutionAction enum — stays]

// ─── Helpers ─────────────────────────────────────────────────────────────────
[handle_execution_failure — stays, already extracted in Phase 1]

pub mod accumulator;
pub mod worker;
pub mod batch_loop;
pub mod pool;

pub use accumulator::PerPartitionBuffer;
pub use worker::worker_loop;
pub use batch_loop::{batch_worker_loop, handle_batch_result_inline};
pub use pool::WorkerPool;
```
</action>
  <verify>
    <automated>cargo check --lib 2>&1 | tail -5 && wc -l /home/nghiem/project/KafPy/src/worker_pool/mod.rs</automated>
  </verify>
  <done>WorkerPool in pool.rs, thin mod.rs under 60 lines, all 4 modules declared, all re-exports present, cargo check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {worker_pool/mod.rs} → {worker_pool/worker.rs} | worker_loop called across module boundary via re-export |
| {worker_pool/mod.rs} → {worker_pool/pool.rs} | WorkerPool struct crosses boundary |
| {pool.rs} → {worker.rs} | spawn_worker calls worker_loop internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Availability | WorkerPool::shutdown ordering | Accept | Shutdown ordering preserved exactly — no behavioral change |
| T-02-04 | Integrity | JoinSet worker management | Accept | No changes to spawn/shutdown logic — structural only |
| T-02-05 | Consistency | Import path changes | Mitigate | All imports updated atomically in this wave — no cross-wave inconsistency |
</threat_model>

<verification>
Phase-level checks:
1. `cargo check --lib` passes with no errors
2. `grep "async fn worker_loop" src/worker_pool/worker.rs` — exists
3. `grep "async fn batch_worker_loop" src/worker_pool/batch_loop.rs` — exists
4. `grep "async fn handle_batch_result_inline" src/worker_pool/batch_loop.rs` — exists
5. `grep "pub struct WorkerPool" src/worker_pool/pool.rs` — exists
6. `grep -v "^//\|^$\|use \|pub mod\|pub use\|pub struct\|pub enum\|pub fn\|^#\|^!" src/worker_pool/mod.rs | wc -l` — should be 0 or very small (no impl blocks)
7. `wc -l src/worker_pool/mod.rs` — should be under 60 lines
8. `cargo clippy --all-targets 2>&1 | grep -E "^error" | head -5` — no errors
9. `cargo test --lib 2>&1 | tail -3` — all tests pass
</verification>

<success_criteria>
worker_loop in worker.rs, batch_worker_loop + handle_batch_result_inline in batch_loop.rs, WorkerPool in pool.rs, mod.rs is thin re-export layer, all cargo checks pass.
</success_criteria>

<output>
After completion, create .planning/phases/02-split-worker-pool/02-02-SUMMARY.md
</output>
