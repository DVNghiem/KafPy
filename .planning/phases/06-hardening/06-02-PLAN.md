---
phase: 06-hardening
plan: '02'
type: execute
wave: '2'
depends_on:
  - '06-01'
files_modified:
  - src/consumer/context.rs
  - src/dispatcher/mod.rs
  - src/routing/context.rs
  - src/python/execution_result.rs
  - src/config.rs
autonomous: true
requirements:
  - LH-06
  - LH-07

must_haves:
  truths:
    - "All public error types implement Debug with full field coverage"
    - "Missing required fields produces a compile-time or build-time error"
  artifacts:
    - path: "src/consumer/context.rs"
      provides: "Context struct with Debug derive added"
      contains: "#[derive(Debug)]"
    - path: "src/config.rs"
      provides: "ProducerConfigBuilder with build-time validation"
      contains: "build()"
  key_links:
    - from: "src/config.rs"
      to: "src/pyconsumer.rs"
      via: "ProducerConfigBuilder::build()"
      pattern: "ProducerConfigBuilder.*build"
---

<objective>
Verify Debug impls on all error-context structs and confirm build-time validation is complete across both Rust and Python config builders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-hardening/06-01-PLAN.md
@.planning/phases/06-hardening/06-RESEARCH.md

From src/config.rs (Rust-side):
```rust
#[derive(Debug, thiserror::Error)]
pub enum BuildError {
    #[error("missing required field: {0}")]
    MissingField(&'static str),
    #[error("at least one topic must be specified")]
    NoTopics,
}
```

From src/consumer/config.rs (Rust-side):
```rust
pub fn build(self) -> Result<ConsumerConfig, BuildError> {
    let brokers = self.brokers.ok_or(BuildError::MissingField("brokers"))?;
    let group_id = self.group_id.ok_or(BuildError::MissingField("group_id"))?;
    if self.topics.is_empty() {
        return Err(BuildError::NoTopics);
    }
    ...
}
```

From src/pyconsumer.rs (Python-facing):
- BuildError already defined with `MissingField` and `NoTopics` variants
- Both ConsumerConfigBuilder and ProducerConfigBuilder have build() methods returning Result

**Research finding:** LH-07 (build-time validation) is already implemented. This plan verifies completeness and adds Debug to any missing structs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Debug derives on error-context structs</name>
  <files>src/consumer/context.rs, src/routing/context.rs, src/python/execution_result.rs</files>
  <action>
Audit all structs used as error context fields and ensure they derive Debug. Based on the RESEARCH.md findings, the following structs need Debug derives added if missing:

1. **src/consumer/context.rs** - Check if `Context` struct has `#[derive(Debug)]`; if not, add it.

2. **src/routing/context.rs** - Check `RoutingContext` struct; add `#[derive(Debug)]` if missing.

3. **src/python/execution_result.rs** - Check `ExecutionResult` struct; add `#[derive(Debug)]` if missing.

Run `cargo check --lib` to verify no Debug derives are missing on public types. Also run `cargo clippy -- -D warnings` to catch any issues.

List the structs examined and whether they already had Debug.
</action>
  <verify>cargo check --lib 2>&1 | head -50 && cargo clippy --lib -- -D warnings 2>&1 | head -80</verify>
  <done>All error-context structs have Debug derive; clippy passes</done>
</task>

<task type="auto">
  <name>Task 2: Verify build-time validation completeness</name>
  <files>src/config.rs, src/consumer/config.rs, src/pyconsumer.rs</files>
  <action>
Verify build-time validation is complete across all builders:

1. **Rust ConsumerConfigBuilder** (src/consumer/config.rs):
   - brokers required → already checked
   - group_id required → already checked
   - topics non-empty → already checked

2. **Rust ProducerConfigBuilder** (src/config.rs):
   - brokers required → needs verification
   - If missing, add validation similar to ConsumerConfigBuilder

3. **Python ConsumerConfigBuilder** (src/pyconsumer.rs):
   - Already has brokers/group_id/topics validation in build()

4. **Python ProducerConfigBuilder** (src/pyconsumer.rs):
   - Already has brokers validation in build()

Check if ProducerConfigBuilder in src/config.rs has build-time validation for brokers. If not, add it.

Run `cargo test --lib build_error 2>&1 | head -30` to verify tests pass for build error cases.
</action>
  <verify>cargo test --lib 2>&1 | grep -E '(test.*build|test.*error|FAILED|passed)'</verify>
  <done>All required fields validated at build time; tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify no regressions</name>
  <files>src/**/*.rs (full project)</files>
  <action>
Run the full test suite to ensure the error struct changes from Wave 1 and Debug additions from Wave 2 do not cause regressions:

```bash
cargo test --lib 2>&1 | tail -20
cargo clippy --lib -- -D warnings 2>&1 | tail -20
```

If any tests fail due to the error struct changes (e.g., test code constructing errors with old string-only syntax), update those tests to use the new structured field syntax.

Also run `cargo fmt` to ensure formatting is correct.
</action>
  <verify>cargo test --lib 2>&1 | tail -10 && cargo clippy --lib -- -D warnings 2>&1 | tail -10</verify>
  <done>Full test suite passes; clippy clean; formatting correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build time | Required field validation happens here; missing brokers/group_id caught early |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial | Missing broker config at build time | mitigate | BuildError::MissingField("brokers") returned if not set |
</threat_model>

<verification>
cargo test --lib 2>&1 | tail -10
cargo clippy --lib -- -D warnings 2>&1 | tail -10
</verification>

<success_criteria>
1. All error-context structs have Debug derive (Context, RoutingContext, ExecutionResult)
2. ProducerConfigBuilder::build() validates brokers are set
3. ConsumerConfigBuilder::build() validates brokers, group_id, and topics non-empty
4. All tests pass
5. Clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening/06-02-SUMMARY.md`
</output>