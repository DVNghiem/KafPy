---
phase: 05-builder-pattern-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/consumer/config.rs
autonomous: true
requirements:
  - LH-04

must_haves:
  truths:
    - "Builder rejects missing brokers with clear error"
    - "Builder rejects missing group_id with clear error"
    - "Builder rejects empty topics list with clear error"
    - "Builder accepts valid configs and produces correct ConsumerConfig"
    - "Builder accepts valid configs and produces correct ProducerConfig"
  artifacts:
    - path: "src/consumer/config.rs"
      provides: "#[cfg(test)] module with builder unit tests"
      min_lines: 50
  key_links:
    - from: "test module"
      to: "ConsumerConfigBuilder"
      via: "unit test exercises build() method"
    - from: "test module"
      to: "ProducerConfigBuilder"
      via: "unit test exercises build() method"
---

<objective>
Add unit tests for the ConsumerConfigBuilder and ProducerConfigBuilder build() validation logic, covering required-field errors and edge cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-builder-pattern-refactor/05-01-SUMMARY.md
@src/consumer/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add builder unit tests to src/consumer/config.rs</name>
  <files>src/consumer/config.rs</files>
  <action>
Add a `#[cfg(test)]` module at the bottom of src/consumer/config.rs (inside the module, after the last impl block). Use `use super::*;` to bring the pyclass types into scope.

Test cases for ConsumerConfigBuilder:
- `test_consumer_builder_requires_brokers` — call `ConsumerConfigBuilder::new().group_id("g").topics(["t"]).build()`, expect Err with message containing "brokers"
- `test_consumer_builder_requires_group_id` — call `ConsumerConfigBuilder::new().brokers("x").topics(["t"]).build()`, expect Err with message containing "group_id"
- `test_consumer_builder_requires_topics` — call `ConsumerConfigBuilder::new().brokers("x").group_id("g").build()`, expect Err with message containing "topics"
- `test_consumer_builder_success_with_minimal_fields` — `ConsumerConfigBuilder::new().brokers("x").group_id("g").topics(["t"]).build()` succeeds and produces a ConsumerConfig with those values
- `test_consumer_builder_with_all_fields` — chain all builder methods and verify build() succeeds

Test cases for ProducerConfigBuilder:
- `test_producer_builder_requires_brokers` — call `ProducerConfigBuilder::new().build()`, expect Err with message containing "brokers"
- `test_producer_builder_success_with_brokers` — `ProducerConfigBuilder::new().brokers("x").build()` succeeds and produces a ProducerConfig

For the Python errors, PyErr message content can be checked by converting to string via `py_err.to_string()`.

Note: Since these are unit tests in a pyclass module, they run against the Python-bound structs directly. Use the standard Rust test pattern with `#[test]` attributes inside `#[cfg(test)] mod tests { ... }`.
</action>
  <verify>cargo test --lib 2>&1 | tail -20</verify>
  <done>All builder unit tests pass: required field validation, success paths</done>
</task>

</tasks>

<verification>
cargo test --lib 2>&1 | grep -E "test result|builder|config" | head -20
</verification>

<success_criteria>
- Unit tests exist for ConsumerConfigBuilder and ProducerConfigBuilder
- Tests cover required-field missing errors (brokers, group_id, topics)
- Tests cover successful build with minimal fields
- cargo test --lib passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-builder-pattern-refactor/05-02-SUMMARY.md`
</output>
