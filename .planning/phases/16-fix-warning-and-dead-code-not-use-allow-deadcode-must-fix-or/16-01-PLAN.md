---
phase: 16
plan: 01
type: refactor
wave: 1
depends_on: 15
files_modified:
  - src/pyconsumer.rs
  - src/worker_pool/streaming_loop.rs
  - src/worker_pool/worker.rs
  - src/worker_pool/fan_in_loop.rs
  - src/dispatcher/queue_manager.rs
  - src/observability/metrics.rs
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "cargo build 2>&1 | grep -E 'error|warning:' shows zero warnings"
    - "cargo clippy --all-targets 2>&1 shows zero warnings"
    - "cargo test 2>&1 passes"
    - "No #[allow(dead_code)] or #[allow(unused)] attributes present in src/"
    - "No dead code (unused functions, constants, fields) remains"
    - "No deprecated API calls remain"
    - "No pub function uses pub(crate) types incorrectly"
  artifacts:
    - path: "src/pyconsumer.rs"
      fix: "Python::assume_gil_acquired() → Python::assume_attached() at lines 289 and 334"
    - path: "src/worker_pool/streaming_loop.rs"
      fix: "Unused variables prefixed with _ or removed; pub → pub(crate) on line 49"
    - path: "src/worker_pool/worker.rs"
      fix: "Unused fields use .. pattern; unused variables prefixed with _"
    - path: "src/worker_pool/fan_in_loop.rs"
      fix: "pub → pub(crate) on line 35"
    - path: "src/dispatcher/queue_manager.rs"
      fix: "Dead code removed: STREAMING_BUFFER_CAPACITY, paused_partitions field, pause_partition/resume_partition/is_partition_paused methods"
    - path: "src/observability/metrics.rs"
      fix: "Dead code removed: record_branch_duration function"
  key_links: []
---

<objective>
Fix all compiler warnings and remove all dead code. The goal is a clean build with zero warnings, zero dead code, and no suppression attributes (no `#[allow(dead_code)]` or `#[allow(unused)]`).

This phase is a cleanup/refactor at the end of the v2.0 milestone. No new features are being added. Existing requirements (FANOUT-*, FANIN-*, OBSV-*) remain unchanged.
</objective>

<context>
@src/pyconsumer.rs (lines 289, 334)
@src/worker_pool/streaming_loop.rs (lines 51, 62, 71, 123, 132)
@src/worker_pool/worker.rs (lines 369, 435, 452, 478)
@src/worker_pool/fan_in_loop.rs (line 35)
@src/dispatcher/queue_manager.rs (lines 20, 147, 243-262)
@src/observability/metrics.rs (line 360)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyconsumer.rs deprecated API calls</name>
  <files>src/pyconsumer.rs</files>
  <action>
Read src/pyconsumer.rs around lines 285-295 and 330-340 to see the exact context of the two `assume_gil_acquired()` calls.

Replace both occurrences of:
  `unsafe { Python::assume_gil_acquired() }`
with:
  `unsafe { Python::assume_attached() }`

The deprecated `assume_gil_acquired()` method has been replaced by `assume_attached()` in newer PyO3 versions. Both are unsafe and require the GIL to be held.
  </action>
  <verify>
cargo clippy --all-targets 2>&1 | grep -E "assume_gil_acquired|deprecated" | head -20
  </verify>
  <done>Both lines 289 and 334 use `Python::assume_attached()` instead of the deprecated `assume_gil_acquired()`</done>
</task>

<task type="auto">
  <name>Task 2: Fix streaming_loop.rs unused variables and visibility</name>
  <files>src/worker_pool/streaming_loop.rs</files>
  <action>
Read src/worker_pool/streaming_loop.rs fully.

1. Change `pub async fn streaming_worker_loop` (line ~49) to `pub(crate) async fn streaming_worker_loop` — the function is internal to the worker_pool crate but was marked pub. It uses `QueueManager` which is pub(crate), so the function visibility must be at most pub(crate).

2. Fix unused variables:
   - Line ~51: The `consumer` parameter is never used. Since removing it would change the function signature (potentially breaking callers), prefix it with `_`: `consumer: Arc<_>` or just verify if it can be removed entirely by checking who calls this function. Check if any code path passes a consumer that is actually used. If truly unused and signature can't change, use `_consumer` prefix.
   - Line ~62: `current_attempt: u32` is assigned but never read. This is the recovery attempt counter that SHOULD be decremented/checked in the Recovering state. Looking at the code, `state = StreamingState::Recovering { attempt: 1 }` sets attempt to 1, but `current_attempt` is a separate variable assigned but never used. Since `attempt` inside the Recovering state enum variant is what actually drives the state machine, `current_attempt` variable at line 62 is redundant dead code. Remove it.
   - Line ~71: `ctx` is created but never used (the invoke_streaming call uses a different ctx built inside the loop at line 107-112). Remove the `let ctx = ...` block at lines 71-76.
   - Line ~123: In `ExecutionResult::Error { reason, exception, traceback }`, the `reason` field is unused. Change to `ExecutionResult::Error { reason: _, exception, traceback }` to ignore the field.
   - Line ~132: `current_attempt = 1` assignment. Since we removed `current_attempt` variable above, this assignment is now meaningless. Remove it.

Note: When removing the `ctx` block at lines 71-76, ensure the `let mut state = StreamingState::Starting;` that precedes it remains. The state machine starts in Starting and transitions to Running immediately.
  </action>
  <verify>
cargo clippy --all-targets 2>&1 | grep -E "streaming_loop|unused|warning" | head -20
  </verify>
  <done>
- streaming_worker_loop is pub(crate), not pub
- consumer param has underscore prefix or is removed from signature
- current_attempt variable and assignment removed
- ctx block removed
- reason field ignored in ExecutionResult::Error pattern
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix worker.rs unused variables</name>
  <files>src/worker_pool/worker.rs</files>
  <action>
Read src/worker_pool/worker.rs around lines 369, 435, 452, 478.

1. Line 369: `parent_trace_flags_opt` is assigned but never read. Prefix with underscore: `let _parent_trace_flags_opt = trace_flags.clone();`

2. Line 435: In `ExecutionResult::Rejected { reason, .. }` pattern match, the `reason` field is unused. Change to `ExecutionResult::Rejected { reason: _, .. }`.

3. Line 452: `msg_topic` is created (line 452) but never used. Check lines 457-490 to confirm. Looking at the code snippet, `msg_topic` is cloned for the dlq task spawn at line 452, but the subsequent dlq code path might not use it. Actually looking at the span, `msg_topic` is not used in the async block that follows (lines 457+). Remove `let msg_topic = msg.topic.clone();` at line 452.

4. Line 478: `is_timeout` is assigned in a match but the variable is not used afterward (the value is only used inside the match arm for constructing dlq metadata). Remove `is_timeout` from the pattern and just use `_` or restructure.
  </action>
  <verify>
cargo clippy --all-targets 2>&1 | grep -E "worker\.rs|unused|warning" | head -20
  </verify>
  <done>
- parent_trace_flags_opt has underscore prefix
- reason field in Rejected pattern ignored with _
- msg_topic variable removed
- is_timeout handled correctly (either removed or used)
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix fan_in_loop.rs and streaming_loop.rs pub visibility</name>
  <files>src/worker_pool/fan_in_loop.rs, src/worker_pool/streaming_loop.rs</files>
  <action>
Read src/worker_pool/fan_in_loop.rs around line 35.

The function `pub async fn fan_in_worker_loop` at line 35 is marked `pub` but uses `pub(crate)` type `QueueManager` as a parameter. In Rust, a pub function cannot have pub(crate) types in its signature (pub(crate) is more restrictive than pub). Change `pub async fn fan_in_worker_loop` to `pub(crate) async fn fan_in_worker_loop`.

Similarly check streaming_loop.rs line 49 — change `pub async fn streaming_worker_loop` to `pub(crate) async fn streaming_worker_loop` if not already done in Task 2.
  </action>
  <verify>
cargo build 2>&1 | grep -E "private_interface|warning|pub.*pub\(crate\)" | head -20
  </verify>
  <done>
- fan_in_worker_loop is pub(crate)
- streaming_worker_loop is pub(crate) (from Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 5: Remove dead code from queue_manager.rs</name>
  <files>src/dispatcher/queue_manager.rs</files>
  <action>
Read src/dispatcher/queue_manager.rs fully to understand the full context.

**Dead code to remove:**

1. `STREAMING_BUFFER_CAPACITY` constant (line 20): This constant is defined but never used by any code in src/ (only referenced in planning docs). Remove lines 18-20 (the comment and const declaration).

2. `paused_partitions` field in `HandlerEntry` (line 147): This field is set during `register_handler_with_semaphore` but never read by any code in src/. Remove line 147 entirely.

3. `pause_partition` method (lines 243-249): Never called from src/ (only defined here). Remove.

4. `resume_partition` method (lines 253-259): Never called from src/. Remove.

5. `is_partition_paused` method (lines 262-268): Never called from src/. Remove.

Note: The `pause_partition`/`resume_partition` methods are part of the backpressure design in Phase 10/15, but the QueueManager-level tracking (this HashSet-based tracking in HandlerEntry) is separate from the ConsumerRunner-level Kafka pause/resume. The QueueManager methods are the ones that need removal — they track paused partitions in a HashSet but nothing reads that HashSet. The actual Kafka consumer pause/resume happens at the ConsumerDispatcher level. This removal does NOT affect the actual Kafka pause functionality.

Also remove from the `HandlerEntry` construction at line ~189 the `paused_partitions: ...` field initialization, since you removed the field at line 147.
  </action>
  <verify>
cargo clippy --all-targets 2>&1 | grep -E "queue_manager|dead_code|warning" | head -20 && cargo build 2>&1 | grep -E "error" | head -10
  </verify>
  <done>
- STREAMING_BUFFER_CAPACITY constant removed
- paused_partitions field removed from HandlerEntry
- pause_partition/resume_partition/is_partition_paused methods removed
- HandlerEntry construction updated (no paused_partitions initialization)
  </done>
</task>

<task type="auto">
  <name>Task 6: Remove dead code from metrics.rs</name>
  <files>src/observability/metrics.rs</files>
  <action>
Read src/observability/metrics.rs around line 360.

The `FanOutMetrics::record_branch_duration` function is defined but never called from any code in src/. This was planned for future fan-out metrics but is not currently used. Remove the entire `record_branch_duration` function (lines 359-380 approximately). Verify the exact line range by reading the surrounding context.

Make sure to remove the entire function body including its closing brace, and any trailing blank lines, leaving the rest of FanOutMetrics intact.
  </action>
  <verify>
cargo clippy --all-targets 2>&1 | grep -E "metrics|dead_code|warning" | head -20
  </verify>
  <done>
- record_branch_duration function completely removed from FanOutMetrics
  </done>
</task>

<task type="auto">
  <name>Task 7: Verify clean build and zero warnings</name>
  <files>src/pyconsumer.rs, src/worker_pool/streaming_loop.rs, src/worker_pool/worker.rs, src/worker_pool/fan_in_loop.rs, src/dispatcher/queue_manager.rs, src/observability/metrics.rs</files>
  <action>
After all previous tasks, run the full verification suite:

1. `cargo build 2>&1 | grep -E "error|warning:"` — must show zero warnings
2. `cargo clippy --all-targets 2>&1 | grep -E "warning|error"` — must show zero warnings
3. `cargo test 2>&1` — must pass
4. `grep -rn "allow(dead_code)" src/` — must return no results
5. `grep -rn "allow(unused)" src/` — must return no results

If any issues remain, fix them and repeat until clean.

Note: The benchmark module (src/benchmark/runner.rs) is explicitly out of scope for this phase — it has structural issues unrelated to warning fixes and will be handled separately (Phase 41/42). Do NOT touch it.
  </action>
  <verify>
cargo build 2>&1 | grep -E "error|warning:" | wc -l
# Must output 0

cargo clippy --all-targets 2>&1 | grep -c "warning:"
# Must output 0

cargo test 2>&1 | tail -5
# Must show test success
  </verify>
  <done>
- cargo build shows zero warnings
- cargo clippy shows zero warnings
- cargo test passes
- No #[allow(dead_code)] in src/
- No #[allow(unused)] in src/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — This phase is a refactor/cleanup with no external I/O or user-facing changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-16-01 | None | Cleanup/refactor | N/A | No threats applicable — internal refactoring only |
</threat_model>

<verification>
After all tasks complete:

```bash
cargo build 2>&1 | grep -E "warning:" | wc -l   # Must be 0
cargo clippy --all-targets 2>&1 | grep -c "warning:"  # Must be 0
cargo test 2>&1 | tail -5  # Must pass
grep -rn "allow(dead_code)" src/ | wc -l  # Must be 0
grep -rn "allow(unused)" src/ | wc -l     # Must be 0
```

The benchmark module (src/benchmark/runner.rs) is out of scope and will not be addressed in this phase.
</verification>

<success_criteria>
- Zero compiler warnings from `cargo build`
- Zero clippy warnings from `cargo clippy --all-targets`
- All tests pass with `cargo test`
- No `#[allow(dead_code)]` or `#[allow(unused)]` attributes in src/
- No dead code (unused functions, constants, fields) remains
- All deprecated API calls replaced
- All visibility errors fixed (pub → pub(crate) for internal functions using pub(crate) types)
</success_criteria>

<output>
After completion, create `.planning/phases/16-fix-warning-and-dead-code-not-use-allow-deadcode-must-fix-or/16-01-SUMMARY.md`
</output>